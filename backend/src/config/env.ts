import { CONFIG } from "./index";

export const env = {
  APP_ENV: CONFIG.appEnv,
  NODE_ENV: CONFIG.nodeEnv,
  PORT: CONFIG.port,
  API_RATE_LIMIT_MAX: CONFIG.apiRateLimitMax,
  CLIENT_URL: CONFIG.clientUrl,
  MONGO_URI: CONFIG.mongoUri,
  REDIS_URL: CONFIG.redisUrl,
  REDIS_ENABLED: CONFIG.redisEnabled,
  KAFKA_ENABLED: CONFIG.kafkaEnabled,
  KAFKA_BROKERS: CONFIG.kafkaBroker,
  KAFKA_DEFAULT_PARTITIONS: CONFIG.kafkaDefaultPartitions,
  KAFKA_SYMBOL_EVENT_PARTITIONS: CONFIG.kafkaSymbolEventPartitions,
  KAFKA_PORTFOLIO_EVENT_PARTITIONS: CONFIG.kafkaPortfolioEventPartitions,
  ANALYTICS_CONSUMER_GROUP: CONFIG.analyticsConsumerGroup,
  KAFKA_SASL_MECHANISM: CONFIG.kafkaSaslMechanism,
  KAFKA_SASL_USERNAME: CONFIG.kafkaSaslUsername,
  KAFKA_SASL_PASSWORD: CONFIG.kafkaSaslPassword,
  JWT_SECRET: CONFIG.jwtSecret,
  CURSOR_SIGNING_SECRET: CONFIG.cursorSigningSecret,
  ALPHA_VANTAGE_KEY: CONFIG.alphaVantageKey,
  GOOGLE_CLIENT_ID: CONFIG.googleClientId,
  FMP_API_KEY: CONFIG.fmpApiKey,
  AWS_REGION: CONFIG.awsRegion,
  AWS_S3_BUCKET: CONFIG.awsS3Bucket,
  AWS_ACCESS_KEY_ID: CONFIG.awsAccessKeyId,
  AWS_SECRET_ACCESS_KEY: CONFIG.awsSecretAccessKey,
  AWS_CDN_BASE_URL: CONFIG.awsCdnBaseUrl,
  LOG_REQUEST_SAMPLE_RATE: Math.max(0, Math.min(1, CONFIG.logRequestSampleRate)),
  LOGO_ENRICHMENT_ENABLED: CONFIG.logoEnrichmentEnabled,
  LOGO_ENRICHMENT_INTERVAL_MS: CONFIG.logoEnrichmentIntervalMs,
  LOGO_FALLBACK_TARGET_RATIO: CONFIG.logoFallbackTargetRatio,
  CLIENT_URLS: CONFIG.clientUrls,
  CHART_SERVICE_ENABLED: CONFIG.chartServiceEnabled,
  CHART_SERVICE_URL: CONFIG.chartServiceUrl,
  CHART_SERVICE_AUTH_ENABLED: CONFIG.chartServiceAuthEnabled,
  CHART_SERVICE_AUTH_TOKEN: CONFIG.chartServiceAuthToken,
  CHART_SERVICE_TIMEOUT_MS: CONFIG.chartServiceTimeoutMs,
  CHART_SERVICE_RETRY_COUNT: CONFIG.chartServiceRetryCount,
  CHART_SERVICE_RETRY_BASE_MS: CONFIG.chartServiceRetryBaseMs,
  CHART_SERVICE_BREAKER_FAILURE_THRESHOLD: CONFIG.chartServiceBreakerFailureThreshold,
  CHART_SERVICE_BREAKER_FAILURE_WINDOW_MS: CONFIG.chartServiceBreakerFailureWindowMs,
  CHART_SERVICE_BREAKER_COOLDOWN_MS: CONFIG.chartServiceBreakerCooldownMs,
  LOGO_SERVICE_ENABLED: CONFIG.logoServiceEnabled,
  LOGO_SERVICE_MODE: CONFIG.logoServiceMode,
  LOGO_SERVICE_URL: CONFIG.logoServiceUrl,
  DEV_AUTO_START_INFRA: CONFIG.devAutoStartInfra,
  DEV_ALLOW_MEMORY_DB: CONFIG.devAllowMemoryDb,
  DEV_ALLOW_MOCK_REDIS: CONFIG.devAllowMockRedis,
  DEV_DISABLE_KAFKA_IF_UNAVAILABLE: CONFIG.devDisableKafkaIfUnavailable,
  E2E_USE_MEMORY_MONGO: CONFIG.e2eUseMemoryMongo,
  E2E_USE_MOCK_REDIS: CONFIG.e2eUseMockRedis,
  E2E: CONFIG.e2e,
  USD_TO_INR: CONFIG.usdToInr,
};
