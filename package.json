{
  "name": "tradereplay",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "env:check": "node scripts/check-env.js",
    "app": "concurrently -n backend,worker,logo-service,kafka,frontend -c green,yellow,magenta,blue,cyan \"node backend/bootstrap-dev.js\" \"npm --prefix backend run dev:worker\" \"npm --prefix services/logo-service run dev\" \"npm --prefix backend run dev:kafka\" \"npm --prefix frontend run dev\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix services/logo-service install",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "typecheck": "npm --prefix backend run build && npm --prefix frontend run typecheck",
    "migrate": "npm --prefix backend run migrate",
    "seed": "npm --prefix backend run seed",
    "validate:symbols": "npm --prefix backend exec tsx ./tests/integration/validateSymbols.ts",
    "validate": "npm --prefix backend exec tsx ./tests/integration/validateSystem.ts",
    "validate:logo-pipeline": "npm --prefix backend exec tsx ./tests/load/testLogoPipeline.ts",
    "stress:test": "npm --prefix backend exec tsx ./tests/load/stressTest.ts",
    "test:e2e": "playwright test -c tests/integration/e2e/playwright.config.ts",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "deploy:auto": "docker compose up -d redis kafka mongodb backend worker logo-service kafka-service && npm run validate"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "concurrently": "^9.2.1"
  }
}
