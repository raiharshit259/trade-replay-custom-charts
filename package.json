{
  "name": "tradereplay",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "app": "concurrently -n backend,worker,logo-service,kafka,frontend -c green,yellow,magenta,blue,cyan \"npm --prefix backend run dev\" \"npm --prefix backend run dev:worker\" \"npm --prefix services/logo-service run dev\" \"npm --prefix backend run dev:kafka\" \"npm --prefix frontend run dev\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix services/logo-service install",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "typecheck": "npm --prefix backend run build && npm --prefix frontend run typecheck",
    "bench:charts": "npm --prefix packages/tradereplay-charts run bench",
    "migrate": "npm --prefix backend run migrate",
    "seed": "npm --prefix backend run seed",
    "validate:symbols": "npm --prefix backend exec tsx scripts/validateSymbols.ts",
    "test:e2e": "playwright test -c e2e/playwright.config.ts",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "concurrently": "^9.2.1"
  }
}
